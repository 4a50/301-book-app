<!DOCTYPE html>
<html>

<head>
  <title>Update Book</title>
  <link href="/styles/base.css" rel="stylesheet" type="text/css">
</head>

<body>
  <main>
    <form action="/books/update/?_method=PUT" method="POST">

      <input type="text" name="title" value="<%= bookData.title %>">
      <input type="text" name="author" value="<%= bookData.author %>">
      <input type="text" name="isbn" value="<%= bookData.isbn %>">
      <input type="text" name="image_url" value="<%= bookData.image_url %>">
      <input type="hidden" name="id" value="<%=bookData.id%>">

      <textarea type="text" name="description" value="<%= bookData.description %>"><%= bookData.description %>
        </textarea>
      <button type="submit">Update Book</button>
    </form>
  </main>
</body>

</html>